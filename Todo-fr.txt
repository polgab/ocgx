Pour ocgx.tex:

* gérer l'arborescence des OCG (celle afichée par les viewers PDF)

  * pouvoir fournir un nom plus lisible que le nom interne de l'OCG.

  * faire en sorte qu'un sous-OCG soit dans la branche de son OCG
    contenant.

  * par défaut, produire une branche par page (mais, comment faire pour
    les OCG sur plusieurs pages ?).

* créer des OCG ayant du contenu sur plusieurs pages (je crois que c'est
  autorisé par la norme PDF).

* corriger le code modifié/ajouté par P.Gaborit pour permettre la
  création d'un lien (en fait une annotation) *avant* la création du
  groupe correspondant.

* vérifier l'emploi de \the\pageno (TeX) et \thepage (LaTeX). Ce ne sont
  pas des compteurs absolus. Il n'y a donc pas garanti d'unicité...

* si possible, permettre l'ajout de matériel dans un *même* groupe via
  plusieurs appels successifs à \OCG avec le même nom de groupe (si
  c'est faisable).

* ajouter une licence, les auteurs, les dates...

* écrire une documentation d'usage et une documentation du code.

* en faire un package propre (.dtx,.ins,etc.).

* le publier sur CTAN.

Dans demo-ocgx.tex:

* extraire les parties TikZ dans tikz-ocgx

* améliorer le code TikZ

* trouver de meilleurs exemples

* vérifier les droits pour les images curtain* et mosaic*

